function navigateToHomePage() {
    // Check if we are already on the home page
    if (window.location.pathname === '/pages/index.html') {
        alert('You are already on the home page.');
    } else {
        // Redirect to the home page
        window.location.href = '/pages/index.html';
    }
}


function navigateTomodelPage() {
    // Check if we are already on the home page
    if (window.location.pathname === '/pages/models.html') {
        alert('You are already on the models page.');
    } else {
        // Redirect to the home page
        window.location.href = '/pages/models.html';
    }
}


function navigateTolocationPage() {
    // Check if we are already on the home page
    if (window.location.pathname === '/pages/maplocations.html') {
        alert('You are already on the home page.');
    } else {
        // Redirect to the home page
        window.location.href = '/pages/maplocations.html';
    }
}
function navigateTocontactPage() {
    // Check if we are already on the home page
    if (window.location.pathname === '/pages/contact.html') {
        alert('You are already on the home page.');
    } else {
        // Redirect to the home page
        window.location.href = '/pages/contact.html';
    }
}
function navigateToOrderPage() {

        window.location.href = '/pages/orderpage.html';
    
}


